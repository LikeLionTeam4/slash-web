// Mock signed-in user until real auth/settings exist — see Settings > 일반 > 닉네임.
// Already carries its own honorific, so don't append an extra "님" wherever this is used.
export const NICKNAME = '사장님'

export const EMAIL = 'owner@slash.app'
